# S1 — AHBN Small Regression Tests

## Environment

- Project root: `/Users/wwiras/Documents/src/AHBNProj/ahbn/v0.61`
- Interpreter: `/Users/wwiras/Documents/src/AHBNProj/venv0.6/bin/python`
- Repository root: `/Users/wwiras/Documents/src/AHBNProj/ahbn`
- Initial commit inspected: `3f9ff5c9dbadef5a31b06dbffce70a7c3316bff8`
- Initial worktree: clean (`git status --short` had no output)
- No production campaign or production output was run.

## Historical interrupted invocation

The first S1 attempt used:

```text
$ PYTHONDONTWRITEBYTECODE=1 /Users/wwiras/Documents/src/AHBNProj/venv0.6/bin/python /tmp/s1_ahbn_probe.py
Traceback (most recent call last):
  File "/tmp/s1_ahbn_probe.py", line 3, in <module>
    from ahbn.config import load_yaml_config
ModuleNotFoundError: No module named 'ahbn'
```

Cause: the temporary `/tmp` probe was executed without the project root on
Python's import path.

Classification: test invocation/environment failure; not evidence of an AHBN
defect.

Authorized corrective action: retry the unchanged regression intent from the
project root with `PYTHONPATH="$PWD"`.

Algorithm modification: **NONE**.

The full first-attempt record remains preserved in `docs/S1_reg_ahbn.md`.

## Test A — Frozen AHBN Integrity

### Commands

```text
$ git rev-parse --show-toplevel
$ git rev-parse HEAD
$ git log -5 --oneline
$ git status --short
$ sha256sum ahbn/strategies/gossip.py ahbn/strategies/cluster.py ahbn/strategies/dcsoc.py ahbn/strategies/ahbn.py ahbn/control.py ahbn/node.py ahbn/cluster.py ahbn/topology.py ahbn/simulator.py ahbn/failure_injector.py ahbn/churn_manager.py run_one.py run_batch.py configs/exp07_fanout.yaml configs/exp08_ch_bottleneck.yaml configs/exp09_dense_topology.yaml configs/exp10_failure.yaml configs/exp11_churn.yaml configs/exp12_mixed_resources.yaml
```

### Relevant terminal output

```text
/Users/wwiras/Documents/src/AHBNProj/ahbn
3f9ff5c9dbadef5a31b06dbffce70a7c3316bff8
3f9ff5c v0.61 fix dcsoc
05281dc v0.61: S0 Fix Structured semantic 2
e07e02a v0.61: S0 Fix Structured semantic
b23f1c4 v0.61: S0 Fix Gossip semantic additonal
8ddc5b9 v0.61: S0 Fix Gossip semantic
[git status --short: no output]

16ae30348d799475a31b29cba2bc94ee5f5a5ca0dc3a1c8dbfc0d6e91b76ba73  ahbn/strategies/ahbn.py
9a19ae2c9766ea36fe873d4d643cf51d9e8df555b42d2de11d946d30fb60f75f  ahbn/control.py
4968331fb013ac47b1513c7da5c3555b1c0edbfe3c4ee4690f0a604732df2f3a  ahbn/node.py
edd9643e84c85bfd0b7bf015a271a05f0aee43f87e69b5f3b045821e92be939b  ahbn/simulator.py
6246dcf7e05aa1bc0225f4b89bfd203047421891cb2d5d7dc6cd286586959892  run_one.py
e969455101ade3984af62b7f4d77d03f13eb344a9ed5718dd4b42be800a21674  run_batch.py
922c79af378f679a26e6efc23a234f37c6d09e76bc281c3e36b6b62a93b788a2  configs/exp07_fanout.yaml
a9f95f2390484daeeb973e00f0655a3106cd8cfeb1c12e2efda0b02d29ff070c  configs/exp08_ch_bottleneck.yaml
c13035b963582e0b6db28902b645f8536c7723f28cba1fb6179f795abd9b9522  configs/exp09_dense_topology.yaml
```

### Evidence

Static inspection found the canonical controller formulation intact:
`alpha=0.3`, centres `0.5`, weights `(-1,+1,-1,+1)`, `kappa=1`,
`beta=1`, threshold `0.5`, default fanout 3, and bounds `[2,4]`. The EWMA,
centred score, sigmoid, threshold mode decision, and rounded/clamped fanout
formulation remain present.

`docs/S4_finalfreeze_dcsoc.md` predates the explicitly authorized and committed
S0 semantic repairs, and no separate post-repair SHA-256 manifest exists. No
baseline is manufactured here.

Current committed AHBN implementation integrity: **PASS**  
Historical post-repair SHA-256 baseline: **unavailable**  
Unauthorized AHBN modification detected: **NO**

### Result: PASS

## Corrected invocation verification

### Commands

```text
$ pwd
$ /Users/wwiras/Documents/src/AHBNProj/venv0.6/bin/python --version
$ PYTHONDONTWRITEBYTECODE=1 PYTHONPATH="$PWD" /Users/wwiras/Documents/src/AHBNProj/venv0.6/bin/python -c "import ahbn; print('AHBN_IMPORT_OK', ahbn.__file__)"
$ test -f /tmp/s1_ahbn_probe.py && echo 'S1_PROBE_PRESENT /tmp/s1_ahbn_probe.py'
```

### Terminal output

```text
/Users/wwiras/Documents/src/AHBNProj/ahbn/v0.61
Python 3.14.6
AHBN_IMPORT_OK /Users/wwiras/Documents/src/AHBNProj/ahbn/v0.61/ahbn/__init__.py
S1_PROBE_PRESENT /tmp/s1_ahbn_probe.py
```

The import resolved to the required v0.61 project tree. The original temporary
probe was reused without changing its scientific assertions.

## Test B — Controller Activation

### Command

```text
$ PYTHONDONTWRITEBYTECODE=1 PYTHONPATH="$PWD" /Users/wwiras/Documents/src/AHBNProj/venv0.6/bin/python /tmp/s1_ahbn_probe.py
```

### Relevant terminal output

```text
A/C canonical parameters, EWMA/equation, mode and bounds: PASS {'d_hat': 0.3, 'l_hat': 0.15, 'u_hat': 0.0, 'c_hat': 0.3, 'score': 0.15000000000000002, 'weight': 0.5374298453437496, 'mode': 'gossip', 'fanout': 3}
B activation: rows=8 fanout_range=3-3 modes=['gossip'] first_obs=(d=0.000000,l=0.000000,u=0.000000,c=None) first_ewma=(d=0.000000,l=0.000000,u=0.000000,c=0.000000) first_score=0.000000 first_weight=0.500000 first_decision=(gossip,3)
B strategy consumption: calls=8 sample=(0, 'gossip', 3, 3, (1, 2)) PASS
```

### Evidence

The controller was instantiated, eight observation/EWMA/score/decision trace
rows were generated, every mode was valid, and the AHBN forwarding strategy
consumed fanout 3 from the controller decision. No transition was required.

### Result: PASS

## Test C — Runtime Fanout Bound

### Relevant terminal output

```text
A/C canonical parameters, EWMA/equation, mode and bounds: PASS ... fanout: 3
B activation: rows=8 fanout_range=3-3 ... first_decision=(gossip,3)
```

### Evidence

Eight AHBN runtime decisions were inspected before the later Test D stop.
Minimum observed runtime fanout: 3. Maximum observed runtime fanout: 3. The
direct canonical equation probe also produced fanout 3. All observations were
within `[2,4]`. The planned Exp07–Exp09 external-parameter runtime
corroborations were not reached, so the complete Test C scope is incomplete.

### Result: INCOMPLETE

## Test D — Strategy Isolation

### Command

Same corrected probe command shown under Test B.

### Complete relevant terminal output

```text
D gossip: controller=None AHBN_state_updates=0 strategy=GossipStrategy PASS
D cluster: controller=None AHBN_state_updates=0 strategy=ClusterStrategy PASS
Traceback (most recent call last):
  File "/tmp/s1_ahbn_probe.py", line 137, in <module>
    main()
    ~~~~^^
  File "/tmp/s1_ahbn_probe.py", line 97, in main
    other, other_source = build_simulation_from_config(neutral, name)
                          ~~~~~~~~~~~~~~~~~~~~~~~~~~~~^^^^^^^^^^^^^^^
  File "/Users/wwiras/Documents/src/AHBNProj/ahbn/v0.61/run_one.py", line 137, in build_simulation_from_config
    strategy = DCSOCStrategy(
        fanout=int(
    ...<13 lines>...
        ),
    )
TypeError: DCSOCStrategy() takes no arguments
```

### Failing check and likely cause

The DC-SoC isolation sentinel could not construct the comparator through the
`run_one.py` configuration factory. The current `run_one.py` DC-SoC branch
passes `fanout` and `inter_fanout` constructor arguments, while the current
`DCSOCStrategy` accepts no constructor arguments.

Classification: current runner/comparator construction-path incompatibility.
The failure occurs before a DC-SoC simulation and is not evidence that DC-SoC
invoked AHBN control. It is also not the prior import-path problem.

Per the S1 stop rule, no alternate construction, retry, source repair, or
assertion change was attempted.

### Result: FAIL

## Test E — Exp07 Wiring

### Preserved static command

```text
$ PYTHONDONTWRITEBYTECODE=1 /Users/wwiras/Documents/src/AHBNProj/venv0.6/bin/python -m scripts.validate_stage4_exp07_execution
```

### Preserved terminal output

```text
Gossip fixed-fanout sweep: [2, 3, 4, 5, 6]
Gossip scheduled runs    : 100
AHBN scheduled runs      : 20
AHBN receives sweep value: NO
AHBN min_fanout          : 2
AHBN max_fanout          : 4
AHBN default_fanout      : 3
AHBN result fanout       : None
Expected total runs      : 120
EXP07 EXECUTION VALIDATION: PASS
```

Runtime corroboration was not reached after Test D failed.

### Result: INCOMPLETE

## Test F — Exp08 Wiring

Not run after Test D failure.

### Result: NOT RUN

## Test G — Exp09 Wiring

Not run after Test D failure.

### Result: NOT RUN

## S1 Final Sign-off

```text
Test    Purpose                         Result
A       Frozen implementation           PASS
B       Controller activation           PASS
C       Runtime fanout [2,4]             INCOMPLETE
D       Strategy isolation               FAIL
E       Exp07 wiring                     INCOMPLETE
F       Exp08 wiring                     NOT RUN
G       Exp09 wiring                     NOT RUN
```

```text
Frozen AHBN unchanged: YES
Canonical controller invoked by AHBN: YES
AHBN runtime fanout bounded [2,4]: YES FOR 8 OBSERVED DECISIONS; FULL S1 CHECK INCOMPLETE
External fanout override of AHBN absent: NOT FULLY VERIFIED
Non-AHBN strategies isolated from AHBN control: NOT FULLY VERIFIED

Exp07 Gossip static sweep preserved: YES
Exp07 AHBN remains adaptive [2,4]: NOT FULLY VERIFIED
Exp08 AHBN remains frozen: NOT FULLY VERIFIED
Exp09 AHBN remains frozen: NOT FULLY VERIFIED

20-seed campaigns executed: NO
Production experiment results generated: NO
Algorithms modified: NO
Comparator semantics modified: NO
```

S1 STATUS: FAIL / INCOMPLETE

The earlier Test B failure was an import-path/test-invocation issue and not an
AHBN defect. The corrected Test B passed. S1 then stopped at the first Test D
failure, a `run_one.py`/`DCSOCStrategy` construction-path incompatibility.
No repair was made.

---

## Test D Failure Diagnosis and Authorized Repair

This section resumes S1 after explicit authorization. The historical failures
and intermediate FAIL/INCOMPLETE sign-off above remain unchanged; the final
sign-off below supersedes that stopped state.

### Original failure

```text
TypeError: DCSOCStrategy() takes no arguments
```

### Pre-edit commands

```text
$ sed -n '1,280p' ahbn/strategies/dcsoc.py
$ sed -n '90,175p' run_one.py
$ sed -n '85,165p' run_batch.py
$ rg -n -C 5 'DCSOCStrategy\(' .
$ rg -n -i -C 2 'inter_fanout|\bfanout\b' ahbn/strategies/dcsoc.py run_one.py run_batch.py scripts configs/exp07_fanout.yaml configs/exp08_ch_bottleneck.yaml configs/exp09_dense_topology.yaml
$ rg -n -i -C 3 'no.*fanout|fanout.*obsolete|DCSOCStrategy\(\)|faithful.*fanout|structural.*obligation' docs/v0.61_S0_fix_dcsoc.md docs/S2_faithful_dcsoc.md docs/S4_finalfreeze_dcsoc.md
```

### Pre-edit evidence

The faithful strategy has no `__init__` and states that explicit structural
relationships are forwarding obligations not subject to a numeric fanout.
Current production paths were:

```text
run_batch.py:157: strategy = DCSOCStrategy()
run_one.py:137:   strategy = DCSOCStrategy(fanout=..., inter_fanout=...)
```

Current Exp08/Exp09 DC-SoC blocks contain only `eps: 2.0` and
`min_samples: 3`. The authorized S0 repair record says the legacy numeric
constructor was removed, `run_batch.py` was changed to `DCSOCStrategy()`, and
the obsolete Exp08/09 fanout fields were removed.

Legacy DC-SoC validator scripts still contain pre-repair constructor calls.
They encode older fixed-budget validation semantics and are not current
production factories or required by S1, so they were documented but not
rewritten in this minimal production-interface repair.

### Root cause

Stale `run_one.py` construction interface after the faithful DC-SoC repair.

### Scientific classification

Runner/comparator integration defect. Not an AHBN algorithm defect. Not a
DC-SoC forwarding-semantic defect.

### Repair

Only `run_one.py` changed:

```diff
-        strategy = DCSOCStrategy(
-            fanout=int(...),
-            inter_fanout=int(...),
-        )
+        strategy = DCSOCStrategy()
```

This is semantically neutral: faithful DC-SoC does not accept or consume
Gossip/AHBN-style numeric forwarding controls. The runner now invokes the
existing faithful interface, matching `run_batch.py`; structural forwarding
obligations remain unchanged.

### Repair verification commands

```text
$ PYTHONDONTWRITEBYTECODE=1 PYTHONPATH="$PWD" /Users/wwiras/Documents/src/AHBNProj/venv0.6/bin/python -c "from ahbn.strategies.dcsoc import DCSOCStrategy; s=DCSOCStrategy(); print(type(s).__name__)"
$ PYTHONDONTWRITEBYTECODE=1 PYTHONPATH="$PWD" /Users/wwiras/Documents/src/AHBNProj/venv0.6/bin/python -c "from ahbn.config import load_yaml_config; from run_one import build_simulation_from_config; cfg=load_yaml_config('configs/sanity_neutral.yaml'); sim, source=build_simulation_from_config(cfg,'dcsoc'); print('DC-SoC construction: PASS', type(sim.strategy).__name__, 'controller=', sim.controller, 'source=', source)"
$ PYTHONDONTWRITEBYTECODE=1 PYTHONPATH="$PWD" /Users/wwiras/Documents/src/AHBNProj/venv0.6/bin/python -c "from ahbn.config import load_yaml_config; from run_one import build_simulation_from_config; cfg=load_yaml_config('configs/sanity_neutral.yaml'); [(lambda sim,name: print(name, type(sim.strategy).__name__, 'controller=' + ('present' if sim.controller is not None else 'None'), 'PASS'))(build_simulation_from_config(cfg,name)[0],name) for name in ('ahbn','gossip','cluster','dcsoc')]"
$ git diff --check
$ git diff -- run_one.py run_batch.py
```

### Terminal output

```text
DCSOCStrategy
DC-SoC construction: PASS DCSOCStrategy controller= None source= 0
ahbn AHBNStrategy controller=present PASS
gossip GossipStrategy controller=None PASS
cluster ClusterStrategy controller=None PASS
dcsoc DCSOCStrategy controller=None PASS
[git diff --check: no output]
```

The diff showed only replacement of the obsolete 17-line DC-SoC argument
construction in `run_one.py` with `DCSOCStrategy()`. `run_batch.py` had no
diff.

### Repair result: PASS

## Resumed Tests C–G

### Command

```text
$ PYTHONDONTWRITEBYTECODE=1 PYTHONPATH="$PWD" /Users/wwiras/Documents/src/AHBNProj/venv0.6/bin/python /tmp/s1_ahbn_probe.py
```

### Complete terminal output

```text
A/C canonical parameters, EWMA/equation, mode and bounds: PASS {'d_hat': 0.3, 'l_hat': 0.15, 'u_hat': 0.0, 'c_hat': 0.3, 'score': 0.15000000000000002, 'weight': 0.5374298453437496, 'mode': 'gossip', 'fanout': 3}
B activation: rows=8 fanout_range=3-3 modes=['gossip'] first_obs=(d=0.000000,l=0.000000,u=0.000000,c=None) first_ewma=(d=0.000000,l=0.000000,u=0.000000,c=0.000000) first_score=0.000000 first_weight=0.500000 first_decision=(gossip,3)
B strategy consumption: calls=8 sample=(0, 'gossip', 3, 3, (1, 2)) PASS
D gossip: controller=None AHBN_state_updates=0 strategy=GossipStrategy PASS
D cluster: controller=None AHBN_state_updates=0 strategy=ClusterStrategy PASS
D dcsoc: controller=None AHBN_state_updates=0 strategy=DCSOCStrategy PASS
E Exp07 AHBN: rows=25 fanout_range=3-3 modes=['cluster', 'gossip'] first_obs=(d=0.000000,l=0.000000,u=0.000000,c=None) first_ewma=(d=0.000000,l=0.000000,u=0.000000,c=0.000000) first_score=0.000000 first_weight=0.500000 first_decision=(gossip,3)
F Exp08 AHBN: rows=25 fanout_range=3-3 modes=['cluster', 'gossip'] first_obs=(d=0.000000,l=0.000000,u=0.000000,c=None) first_ewma=(d=0.000000,l=0.000000,u=0.000000,c=0.000000) first_score=0.000000 first_weight=0.500000 first_decision=(gossip,3)
G Exp09 AHBN p=0.3: rows=31 fanout_range=3-3 modes=['cluster', 'gossip'] first_obs=(d=0.000000,l=0.000000,u=0.000000,c=None) first_ewma=(d=0.000000,l=0.000000,u=0.000000,c=0.000000) first_score=0.000000 first_weight=0.500000 first_decision=(gossip,3)
G Exp09 AHBN p=0.6: rows=37 fanout_range=3-3 modes=['cluster', 'gossip'] first_obs=(d=0.000000,l=0.000000,u=0.000000,c=None) first_ewma=(d=0.000000,l=0.000000,u=0.000000,c=0.000000) first_score=0.000000 first_weight=0.500000 first_decision=(gossip,3)
C all observed AHBN fanouts: min=3 max=3 PASS
S1_RUNTIME_PROBES: PASS
```

### Test C — Runtime Fanout Bound: PASS

The resumed probe inspected 126 AHBN runtime decisions: 8 activation, 25
Exp07, 25 Exp08, 31 Exp09 at `p=0.3`, and 37 Exp09 at `p=0.6`. Minimum and
maximum were both 3; all satisfied `[2,4]`. External sweep, overload, and
density values did not replace the controller decision.

### Test D — Strategy Isolation: PASS

Gossip, Structured, and DC-SoC ran through the real factory with
`controller=None`, zero AHBN-state updates, and their own strategy class.

### Test E — Exp07 Wiring: PASS

The preserved static execution validator remains PASS. Runtime factory
corroboration command and output:

```text
$ PYTHONDONTWRITEBYTECODE=1 PYTHONPATH="$PWD" /Users/wwiras/Documents/src/AHBNProj/venv0.6/bin/python -c "from ahbn.config import load_yaml_config; from run_one import build_simulation_from_config; from run_batch import build_ahbn_strategy,build_ahbn_params; cfg=load_yaml_config('configs/exp07_fanout.yaml'); cfg.update(num_nodes=12,ba_m=2,use_topology_cache=False); ..."
Exp07 Gossip runtime factory k= 2 strategy_fanout= 2 PASS
Exp07 Gossip runtime factory k= 6 strategy_fanout= 6 PASS
Exp07 AHBN runtime factory adaptive= True default= 3 bounds= (2, 4) PASS
```

The AHBN Exp07 smoke generated 25 decisions, used both valid modes, and
remained at adaptive fanout 3. Gossip's static pressure remained separate.

### Test F — Exp08 Wiring: PASS

The minimal smoke used `ch_overload_factor=3.0`, generated 25 AHBN decisions,
used both modes, and remained within `[2,4]`. The Exp08 AHBN block equalled
`AHBNParams()` exactly. Static path inspection showed the overload factor is
passed to `Simulator`, where it adds environment delay; normal normalized
observations then feed `update_metrics` and `decide_mode_and_fanout`. No
Exp08/overload-specific AHBN tuning branch exists.

### Test G — Exp09 Wiring: PASS

Minimal density smokes at `p=0.3` and `p=0.6` generated 31 and 37 AHBN
decisions. Canonical parameters were unchanged and every fanout was 3.
Explicit topology corroboration:

```text
Exp09 ER p= 0.3 nodes= 12 edges= 24
Exp09 ER p= 0.6 nodes= 12 edges= 42
```

Thus `p` changed the ER topology/environment, not AHBN configuration. No
density-specific AHBN tuning or fixed-fanout override exists.

## Final Regression and Diff Check

### Commands

```text
$ git diff --check
$ git status --short
$ git diff --stat
$ git diff -- run_one.py run_batch.py
$ git diff -- ahbn/control.py ahbn/strategies/ahbn.py
```

### Terminal output

```text
[git diff --check: no output]
 M run_one.py
?? docs/v0.61_S1_reg_ahbn.md
 v0.61/run_one.py | 18 +-----------------
 1 file changed, 1 insertion(+), 17 deletions(-)
[run_one.py diff: stale DCSOCStrategy arguments replaced by DCSOCStrategy()]
[run_batch.py diff: none]
[ahbn/control.py and ahbn/strategies/ahbn.py diff: none]
```

Authorized production repair: `run_one.py` only.  
Audit documentation: `docs/v0.61_S1_reg_ahbn.md`.  
Frozen AHBN implementation modified by S1 repair: **NO**.  
Runner/interface compatibility repaired: **YES**.  
Comparator dissemination semantics modified: **NO**.

## Final S1 Matrix — Superseding Sign-off

```text
Test    Purpose                         Result
A       Frozen implementation           PASS
B       Controller activation           PASS
C       Runtime fanout [2,4]             PASS
D       Strategy isolation               PASS
E       Exp07 wiring                     PASS
F       Exp08 wiring                     PASS
G       Exp09 wiring                     PASS
```

```text
Frozen AHBN unchanged: YES
Canonical controller invoked by AHBN: YES
AHBN runtime fanout bounded [2,4]: YES
External fanout override of AHBN absent: YES
Non-AHBN strategies isolated from AHBN control: YES

Exp07 Gossip static sweep preserved: YES
Exp07 AHBN remains adaptive [2,4]: YES
Exp08 AHBN remains frozen: YES
Exp09 AHBN remains frozen: YES

DC-SoC runner construction repaired: YES
DC-SoC dissemination semantics changed: NO

20-seed campaigns executed: NO
Production experiment results generated: NO
AHBN algorithm modified: NO
Comparator algorithms modified: NO
Comparator dissemination semantics modified: NO
```

S1 AHBN SMALL REGRESSION TESTS: PASS

The stale DC-SoC runner construction interface was repaired without changing
DC-SoC dissemination semantics.

AHBN remains frozen, isolated, actively controlled by its canonical
controller, and bounded to fanout `[2,4]`.

Exp07–Exp09 AHBN wiring is verified.

No 20-seed campaign was started.  
No production results were generated.

S1 CLOSED.
