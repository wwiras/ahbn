# S1 — Gossip Small Regression Tests

## Environment

- Project: `/Users/wwiras/Documents/src/AHBNProj/ahbn/v0.61`
- Interpreter: `/Users/wwiras/Documents/src/AHBNProj/venv0.6/bin/python`
- Python: `3.14.6`
- Date: 2026-08-22
- Time zone: Asia/Kuala_Lumpur
- Scope: deterministic construction/runtime probes only; no production experiment campaign

Final hygiene commands:

```text
git diff --check
git status --short
```

Terminal output:

```text
# git diff --check: no output
?? docs/v0.61_S1_reg_gossip.md
?? scripts/validate_s1_gossip.py
```

## Test A — Gossip Implementation Integrity

### Commands

```text
sed -n '1,240p' ahbn/strategies/gossip.py
sed -n '1,300p' run_one.py
sed -n '1,660p' run_batch.py
sed -n '1,300p' ahbn/simulator.py
sed -n '1,280p' configs/exp07_fanout.yaml
sed -n '1,280p' configs/exp08_ch_bottleneck.yaml
sed -n '1,280p' configs/exp09_dense_topology.yaml
sed -n '1,280p' scripts/validate_s0_gossip.py
sed -n '1,280p' scripts/validate_s0_gossip2.py
rg -n "^def exp08|^def exp09|fanout=|select_targets|controller|adaptive" run_batch.py ahbn/simulator.py ahbn/strategies/gossip.py configs/exp0{7,8,9}*.yaml
```

### Terminal Output

Relevant search output:

```text
run_batch.py:97:    controller = None
run_batch.py:100:        strategy = GossipStrategy(fanout=fanout)
run_batch.py:339:def exp08(cfg: dict) -> tuple[list[ResultRow], list]:
run_batch.py:406:def exp09(cfg: dict) -> tuple[list[ResultRow], list]:
run_batch.py:442:                    fanout=None,
ahbn/simulator.py:732:            targets = self.strategy.select_targets(
ahbn/strategies/gossip.py:27:    def select_targets(
configs/exp07_fanout.yaml:20:adaptive_fanout: false
```

### Evidence

Actual path trace:

```text
Exp07 config fanouts [2,3,4,5,6]
  -> run_batch.exp07 loops each fanout
  -> run_single(... strategy_name="gossip", fanout=fanout)
  -> GossipStrategy(fanout=k), controller=None, cluster_manager=None
  -> Simulator.handle_receive(... src_id=immediate sender)
  -> select_targets(... exclude_target_id=src_id)
  -> active physical neighbors excluding sender
  -> sample min(k, eligible count)

Exp08 config has no top-level Gossip fanout
  -> run_batch.exp08 omits fanout
  -> run_single default fanout=None
  -> GossipStrategy(None), controller=None, cluster_manager=None
  -> return every eligible neighbor excluding sender

Exp09 density sweep
  -> run_batch.exp09 passes fanout=None explicitly
  -> GossipStrategy(None), controller=None, cluster_manager=None
  -> density affects graph neighbors only
  -> return every eligible neighbor excluding sender
```

Gossip imports only the message, node, and forwarding-strategy base types. It
does not invoke AHBN, Structured, or DC-SoC logic. Sender exclusion is applied
when constructing candidates, before either the bounded sample or uncapped
return. Exp08 does not create a Gossip cluster head.

### Result

**Test A: PASS**

## Test B — Exp07 Gossip Semantics

### Commands

```text
PYTHONPATH=. PYTHONDONTWRITEBYTECODE=1 /Users/wwiras/Documents/src/AHBNProj/venv0.6/bin/python scripts/validate_s1_gossip.py
```

### Terminal Output

```text
Exp07: seven eligible peers, sender=0
k=2: eligible=7 selected=2 targets=[2, 7] sender_excluded=PASS
k=3: eligible=7 selected=3 targets=[2, 7, 8] sender_excluded=PASS
k=4: eligible=7 selected=4 targets=[2, 4, 7, 8] sender_excluded=PASS
k=5: eligible=7 selected=5 targets=[2, 4, 6, 7, 8] sender_excluded=PASS
k=6: eligible=7 selected=6 targets=[2, 4, 5, 6, 7, 8] sender_excluded=PASS
k=6 shortfall: eligible=3 selected=3 targets=[2, 3, 4] PASS
exp08: eligible=7 selected=7 fanout=None sender_excluded=PASS controller=None updates=0
exp09: eligible=7 selected=7 fanout=None sender_excluded=PASS controller=None updates=0
Exp09 density: low eligible/selected=3/3 high eligible/selected=9/9 PASS
S1 Gossip small regression tests: PASS
```

### k=2

Seven eligible; exactly two selected; sender absent: **PASS**.

### k=3

Seven eligible; exactly three selected; sender absent: **PASS**.

### k=4

Seven eligible; exactly four selected; sender absent: **PASS**.

### k=5

Seven eligible; exactly five selected; sender absent: **PASS**.

### k=6

Seven eligible; exactly six selected; sender absent: **PASS**. With only three
eligible peers, all three were selected, confirming `min(k, eligible)`.

### Sender Exclusion

Sender 0 was absent for every tested fanout: **PASS**.

The configured fanout remained the external static value throughout each run.
No controller, EWMA, latency, utilization, churn, density, or AHBN score altered
it.

### Result

**Test B: PASS**

## Test C — Exp08 Gossip Semantics

### Commands

The S1 validator command and output are recorded under Test B. The validator
uses the actual `exp08()` construction path and real simulator receive path,
while replacing only topology construction and campaign execution.

### Terminal Output

```text
exp08: eligible=7 selected=7 fanout=None sender_excluded=PASS controller=None updates=0
```

### Leakage Check

Exp08 has no top-level `fanout`/`fanouts` control. `exp08()` does not pass a
fanout argument, so `run_single()` receives its default `None`. AHBN fanout
values nested under `ahbn:` are not consumed by Gossip. Exp07 values 2–6 do not
enter the Exp08 Gossip path: **PASS**.

### Sender Exclusion

Sender 0 was absent and all seven eligible peers were selected: **PASS**.

### Result

**Test C: PASS**

## Test D — Exp09 Gossip Semantics

### Commands

The S1 validator command and output are recorded under Test B. It executes the
actual `exp09()` construction path with deterministic low/high-degree graphs.

### Terminal Output

```text
exp09: eligible=7 selected=7 fanout=None sender_excluded=PASS controller=None updates=0
Exp09 density: low eligible/selected=3/3 high eligible/selected=9/9 PASS
```

### Density Behavior

The low-density representative had three eligible neighbors and selected all
three. The high-density representative had nine and selected all nine.
Increasing connectivity therefore naturally increases forwarding opportunity.

### Leakage Check

`exp09()` varies `edge_prob` and explicitly passes `fanout=None`. Probability
changes topology construction only; it does not select a Gossip fanout or
activate the Exp07 sweep: **PASS**.

### Sender Exclusion

Sender 0 was absent in the seven-peer and both density probes: **PASS**.

### Result

**Test D: PASS**

## Test E — Cross-Experiment Isolation

| Experiment | Constructed Gossip fanout | Forwarding rule | Sender excluded |
|---|---:|---|---|
| Exp07 | 2, 3, 4, 5, 6 | sample up to static `k` | YES |
| Exp08 | `None` | all eligible active neighbors | YES |
| Exp09 | `None` | all eligible active neighbors | YES |

### Result

**Test E: PASS**

## Test F — AHBN Isolation Sanity

Every tested Gossip construction recorded `controller=None`,
`cluster_manager=None`, and zero adaptive trace rows. Gossip performs no AHBN
adaptive state update.

### Result

**Test F: PASS**

## Final S1 Gossip Sign-off

| Test | Purpose | Result |
|---|---|---|
| A | Gossip implementation integrity | PASS |
| B | Exp07 static fanout semantics | PASS |
| C | Exp08 normal Gossip semantics | PASS |
| D | Exp09 normal Gossip semantics | PASS |
| E | Cross-experiment isolation | PASS |
| F | AHBN isolation sanity | PASS |

```text
Exp07 k=2 semantics correct: YES
Exp07 k=3 semantics correct: YES
Exp07 k=4 semantics correct: YES
Exp07 k=5 semantics correct: YES
Exp07 k=6 semantics correct: YES

Sender exclusion Exp07: YES
Sender exclusion Exp08: YES
Sender exclusion Exp09: YES

Exp07 fanout sweep active only in Exp07: YES
Exp08 Exp07-sweep leakage absent: YES
Exp09 Exp07-sweep leakage absent: YES

Exp08 normal Gossip dissemination verified: YES
Exp09 normal Gossip dissemination verified: YES

Gossip invokes AHBN controller: NO
Gossip CH invented: NO

20-seed campaigns executed: NO
Production results generated: NO
Algorithms modified: NO
```

```text
S1 GOSSIP SMALL REGRESSION TESTS: PASS

Exp07 retains its intentional static Gossip fanout sweep
k = 2,3,4,5,6 with sender exclusion.

Exp08 and Exp09 use normal Gossip dissemination and do not
inherit the Exp07 fanout-sweep mechanism.

Gossip remains isolated from AHBN control.

No 20-seed campaign was started.
No production experiment results were generated.
No algorithm semantics were modified.

S1 GOSSIP CLOSED.
```
