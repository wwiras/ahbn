# S1 — Structured Small Regression Tests

## Environment

- Project: `/Users/wwiras/Documents/src/AHBNProj/ahbn/v0.61`
- Interpreter: `/Users/wwiras/Documents/src/AHBNProj/venv0.6/bin/python`
- Date: 2026-08-22
- Scope: tiny deterministic Structured probes only; no production campaign.
- Source state after tests: `git status --short` produced no output.

The first validator invocation was:

```text
/Users/wwiras/Documents/src/AHBNProj/venv0.6/bin/python scripts/validate_s0_structured.py
```

It failed before test collection because the project root was not on Python's import path:

```text
Traceback (most recent call last):
  File "/Users/wwiras/Documents/src/AHBNProj/ahbn/v0.61/scripts/validate_s0_structured.py", line 5, in <module>
    from ahbn.config import load_yaml_config
ModuleNotFoundError: No module named 'ahbn'
```

This was an invocation/environment issue only. It was corrected without modifying source:

```text
env PYTHONPATH=. /Users/wwiras/Documents/src/AHBNProj/venv0.6/bin/python scripts/validate_s0_structured.py
```

## Test A — Structured Implementation Integrity

### Commands

```text
rg -n "ClusterStrategy\(|GossipStrategy\(|AHBNController\(|fanout=|fanouts|ch_overload_factor|edge_probs" run_one.py run_batch.py configs/exp08_ch_bottleneck.yaml configs/exp09_dense_topology.yaml
rg -n -i "\[:3\]|first 3|sample\([^\n]*, *3\)|min\([^\n]*, *3\)|fanout" ahbn/strategies/cluster.py ahbn/cluster.py ahbn/topology.py
rg -n "ch_overload_factor|exclude_target_id|select_targets|controller is None|update_ahbn_state" ahbn/simulator.py
```

### Terminal Output

```text
run_one.py:81:        strategy = GossipStrategy(fanout=cfg.get("fanout"))
run_one.py:86:        strategy = ClusterStrategy()
run_one.py:105:        controller = AHBNController(
run_batch.py:100:        strategy = GossipStrategy(fanout=fanout)
run_batch.py:108:        strategy = ClusterStrategy()
run_batch.py:116:        controller = AHBNController(build_ahbn_params(cfg))
run_batch.py:345:    overload_values = cfg["ch_overload_factor"]
run_batch.py:376:                    ch_overload_factor=overload,
run_batch.py:390:                        fanout=None,
run_batch.py:424:    edge_probs = cfg["edge_probs"]
run_batch.py:442:                    fanout=None,

SUSPICIOUS_STRUCTURED_PATHS
ahbn/strategies/cluster.py:34:        fanout: Optional[int] = None,
ahbn/strategies/cluster.py:36:        self.fanout = fanout
ahbn/strategies/cluster.py:131:        if self.fanout is None:
ahbn/strategies/cluster.py:143:            int(self.fanout),
```

### Evidence

The generic optional `fanout` field is not consumed by standalone Structured: both runners instantiate `ClusterStrategy()` without a budget. In `ClusterStrategy._select_head_targets`, `fanout is None` returns all deduplicated active members plus gateways. The bounded branch is documented and used for AHBN bounded Structured execution, not the standalone comparator. No `[:3]`, `first 3`, `sample(..., 3)`, or `min(..., 3)` pattern exists in the inspected Structured construction/forwarding path. `ahbn/cluster.py` only stores and retrieves structural membership/head state. `ahbn/topology.py` constructs the cluster overlay and does not inject a forwarding cap.

Structured does not instantiate `GossipStrategy`, does not construct an AHBN controller, and does not consume the Exp07 fanout loop. Exp08 and Exp09 call `run_single` with no Structured fanout.

### Result

**PASS** — standalone Structured has no hidden Gossip/AHBN-style fanout cap.

## Test B — More-than-Three Structural Forwarding

### Topology

```text
sender 0 -> CH 1 -> structural targets 2,3,4,5,6,7,8
physical-only neighbours of CH 1: 100,101
```

### Legitimate Structural Relationships

Seven obligations were used: five local members and two gateway heads. The immediate sender was excluded.

### Actual Forwarding Targets

```text
eligible=1 selected=1 targets=[2] PASS
eligible=2 selected=2 targets=[2, 3] PASS
eligible=3 selected=3 targets=[2, 3, 4] PASS
eligible=4 selected=4 targets=[2, 3, 4, 5] PASS
eligible=5 selected=5 targets=[2, 3, 4, 5, 6] PASS
eligible=6 selected=6 targets=[2, 3, 4, 5, 6, 7] PASS
eligible=7 selected=7 targets=[2, 3, 4, 5, 6, 7, 8] PASS
```

### Hidden fanout=3 Check

Seven legitimate structural targets produced seven selected targets. Sender 0 and physical-only neighbours 100/101 were absent.

### Result

**PASS**

## Test C — Structural Reachability

### Commands

```text
env PYTHONPATH=. /Users/wwiras/Documents/src/AHBNProj/venv0.6/bin/python scripts/validate_s0_structured.py
```

### Terminal Output

```text
event scheduling targets=[2, 3, 4, 5, 6, 7, 8] PASS
sender exclusion=PASS physical-neighbor isolation=PASS
```

### Reachability Evidence

- Source/sender: 0
- Forwarding node: CH 1
- Legitimate targets: `[2, 3, 4, 5, 6, 7, 8]`
- Actual scheduled targets: `[2, 3, 4, 5, 6, 7, 8]`
- Eventual reachable branches: all seven scheduled structural branches
- Immediate sender selected downstream: no
- Non-structural physical neighbours selected: no

### Result

Required structural edges traversable: **PASS**  
All intended branches reachable: **PASS**

## Test D — Exp08 Structured Semantics

### Baseline Overload

```text
seed=42 factor=1.0 CHs=[0, 1, 2, 3] members=[25, 25, 25, 25] degrees=[30, 11, 14, 15] bottleneck_targets=[0, 1, 2, 3] MATCH=PASS max_CH_targets=26
```

### Increased Overload

```text
seed=42 factor=3.0 CHs=[0, 1, 2, 3] members=[25, 25, 25, 25] degrees=[30, 11, 14, 15] bottleneck_targets=[0, 1, 2, 3] MATCH=PASS max_CH_targets=26
```

### Structural Coverage

The same fixed seed, heads, member counts, physical degrees, and maximum 26 CH structural targets were retained at overload factors 1.0 and 3.0. Simulator overload is applied to delay/processing; the forwarding target selection is unchanged.

### Hidden fanout=3 Check

Maximum selected CH targets was 26 under both conditions. Exp08 passes `fanout=None`; it does not consume Exp07 `k`, Gossip fanout, generic fanout 3, or AHBN `[2,4]` as a Structured limit.

### Result

Exp08 structural coverage preserved: **PASS**  
Hidden fanout=3 absent: **PASS**

## Test E — Exp09 Structured Semantics

### Sparse/Lower-density Condition

```text
edge_prob=0.04 seed=42 CHs=[0, 1, 2, 3] members=[25, 25, 25, 24] degrees=[6, 6, 6, 3] max_CH_targets=26 PASS
```

### Dense/Higher-density Condition

```text
edge_prob=0.12 seed=42 CHs=[0, 1, 2, 3] members=[25, 25, 25, 25] degrees=[14, 13, 14, 11] max_CH_targets=26 PASS
edge_prob=0.12 seed=42 CHs=[0, 1, 2, 3] members=[25, 25, 25, 25] degrees=[14, 13, 14, 11] max_CH_targets=26 PASS
edge_prob=0.12 seed=43 CHs=[0, 1, 2, 3] members=[25, 25, 25, 25] degrees=[11, 12, 10, 15] max_CH_targets=26 PASS
```

### Structural Overlay Evidence

Physical CH degrees increased at higher ER density, but target assertions remained exactly `cluster members excluding CH + gateway heads`. Same-seed dense construction reproduced exactly. Physical-only neighbours were excluded by the direct probe.

### Hidden fanout=3 Check

Exp09 explicitly passes `fanout=None`. Representative CH target counts reached 26, not 3, and were derived from overlay state rather than first/random/all physical neighbours.

### Result

Exp09 structural overlay respected: **PASS**  
Hidden fanout=3 absent: **PASS**  
Physical-density leakage into forwarding rule: **NO**

## Test F — Cross-Experiment Isolation

### Result

| Experiment | Independent variable | Structured rule | Result |
|---|---|---|---|
| Exp08 | CH overload | All legitimate structural targets; overload affects processing/forwarding timing | PASS |
| Exp09 | ER physical density | All legitimate overlay targets; density changes physical environment | PASS |

Exp08 and Exp09 do not impose fanout 3, do not reuse the Exp07 Gossip sweep, and do not apply AHBN `[2,4]` to standalone Structured. **Test F: PASS**

## Test G — Strategy Isolation Sanity

Standalone Structured construction initializes `controller = None`, selects the `cluster` runner branch, constructs `ClusterStrategy()`, and gives the simulator no AHBN controller. `Simulator.update_ahbn_state()` returns immediately when the controller is `None`. No `GossipStrategy` is constructed by the Structured branch.

- Structured AHBN controller: `None`
- Structured AHBN state updates: `0` effective controller updates
- Structured instantiates GossipStrategy: `NO`

### Result

**PASS**

## Final S1 Structured Sign-off

| Test | Purpose | Result |
|---|---|---|
| A | Structured implementation integrity | PASS |
| B | >3 structural forwarding relationships | PASS |
| C | Structural reachability | PASS |
| D | Exp08 Structured semantics | PASS |
| E | Exp09 Structured semantics | PASS |
| F | Cross-experiment isolation | PASS |
| G | Strategy isolation sanity | PASS |

```text
Structured node with >3 legitimate children tested: YES
All legitimate structural children forwarded to: YES
Hidden fanout=3 truncation absent: YES
Required structural paths remain reachable: YES

Exp08 overload changes delay/pressure, not coverage: YES
Exp08 structural forwarding preserved: YES

Exp09 density does not replace structural overlay: YES
Exp09 structural forwarding preserved: YES

Exp07 Gossip sweep leaks into Structured: NO
AHBN [2,4] constrains Structured: NO
Structured invokes AHBN controller: NO

20-seed campaigns executed: NO
Production results generated: NO
Algorithms modified: NO
```

```text
S1 STRUCTURED SMALL REGRESSION TESTS: PASS

Structured forwarding follows all legitimate structural relationships.

A node with more than three legitimate structural forwarding
relationships was verified without hidden fanout=3 truncation.

Exp08 overload affects delay/processing pressure but does not
truncate structural coverage.

Exp09 density changes the physical topology while Structured
continues following its defined structural overlay.

No Exp07 Gossip fanout sweep or AHBN fanout bound leaks into
Structured dissemination.

No 20-seed campaign was started.
No production experiment results were generated.
No algorithm semantics were modified.

S1 STRUCTURED CLOSED.
```
